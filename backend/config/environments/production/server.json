{
  "host": "localhost",
  "port": "${process.env.API_PORT || 1337}",
  "production": true,
  "proxy": {
    "enabled": false
  },
  "cron": {
    "enabled": false
  },
  "admin": {
    "autoOpen": false,
    "build": {
      "backend": "${process.env.API_PROXY_URL}"
    }
  }
}
